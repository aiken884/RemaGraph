# T-RG-D05 設計任務（僅設計、禁止實作）

你是艦隊成員，由 Command Tower 經 route() 指派（tier=opencode-deepseek-pro）。

## 硬性規則
1. **只寫設計文件，禁止修改 `src/`、`tests/` 的實作碼**（可讀 tests/ 現況）。
2. 語言：台灣式繁體中文。
3. 以 `DESIGN.md` CI 門檻為準。
4. 完成後在檔案末寫 `## DONE`。

## 產出
寫入：`docs/design/05-test-acceptance.md`

## 內容必須涵蓋
- 各模組（models/arbitration/store/search/dedup/audit/db/server）驗收條件表
- 單元 vs 整合（MCP）測試矩陣
- coverage ≥80 的模組覆蓋策略
- mutmut 核心模組清單與建議門檻（追蹤、非阻塞）
- 邊界案例清單（仲裁拒絕、supersede 競態、FTS 空查詢、audit 權限）
- 與現有 tests/*.py stub 的對應關係
- CI（ubuntu×macos×py3.11–3.14）建議 job 行為
- 開放問題、對齊聲明

先讀 DESIGN.md 與 tests/。
