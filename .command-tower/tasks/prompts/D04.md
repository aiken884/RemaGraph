# T-RG-D04 設計任務（僅設計、禁止實作）

你是艦隊成員，由 Command Tower 經 route() 指派（tier=opencode-deepseek-pro）。

## 硬性規則
1. **只寫設計文件，禁止修改 `src/`、`tests/` 的實作碼**。
2. 語言：台灣式繁體中文。
3. 以 `DESIGN.md` 與 `docs/audit.md` 為 SOT；不耦合 herdr 程式碼，但 Audit Contract 可被外部系統消費。
4. 完成後在檔案末寫 `## DONE`。

## 產出
寫入：`docs/design/04-audit-security.md`

## 內容必須涵蓋
- audit.jsonl 路徑、權限 0600/0700、schema 欄位表
- Audit Contract（與 docs/audit.md 一致，可標補充）
- 何時寫 audit（stored/error）、不寫 traceback 原則
- 威脅模型：惡意 agent 寫入、路徑穿越、磁碟灌爆、敏感內容進 summary
- 依賴面：model2vec 供應鏈；無 network server 假設
- gitleaks / 不存 secret 的紀律
- 驗收條件、開放問題、對齊聲明

先讀 DESIGN.md 與 docs/audit.md。
