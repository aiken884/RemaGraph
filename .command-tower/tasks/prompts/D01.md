# T-RG-D01 設計任務（僅設計、禁止實作）

你是艦隊成員，由 Command Tower 經 route() 指派（tier=opencode-deepseek-pro）。

## 硬性規則
1. **只寫設計文件，禁止修改 `src/`、`tests/` 的實作碼**（可讀）。
2. 語言：台灣式繁體中文。
3. 以 `/Users/aikenlin/Projects/RemaGraph/DESIGN.md` 為 SOT，不得引入 herdr-bridge / herdr-gov 耦合。
4. 完成後在檔案末寫 `## DONE` 與簡短 checklist。

## 產出
寫入：`docs/design/01-data-model-arbitration.md`

## 內容必須涵蓋
- Memory schema（id/task_id/agent_id/timestamp/kind/summary/learnings/handoff_note/tags/status）精確型別與約束
- 三種 kind：task_handoff / status_update / discovered_constraint 的生命週期
- status_update supersede：同 task_id 精確鍵、併發寫入行為
- discovered_constraint 的 invalidates 機制
- 五條仲裁規則的精確定義、失敗 reason code、回傳 JSON
- Lazy Registration 的 agent_id 規則
- 公開 Python 介面草圖（函式簽名層級，不要寫完整實作）
- 錯誤碼表、邊界案例、Given/When/Then 驗收條件
- 開放問題（供 PPLX）
- 與 DESIGN.md 對齊聲明

先讀 DESIGN.md 與現有 `src/remagraph/models.py`，再寫文件。
