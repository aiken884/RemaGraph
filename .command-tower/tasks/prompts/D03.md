# T-RG-D03 設計任務（僅設計、禁止實作）

你是艦隊成員，由 Command Tower 經 route() 指派（tier=opencode-deepseek-pro）。

## 硬性規則
1. **只寫設計文件，禁止修改 `src/`、`tests/` 的實作碼**。
2. 語言：台灣式繁體中文。
3. 以 `DESIGN.md` 為 SOT；不耦合 herdr。
4. 完成後在檔案末寫 `## DONE`。

## 產出
寫入：`docs/design/03-mcp-server-runtime.md`

## 內容必須涵蓋
- 三個 MCP tool：remagraph_store / remagraph_search / remagraph_status 的完整 request/response JSON schema
- Unix socket daemon 部署形態（比照獨立 MCP server）
- 啟動參數、環境變數、socket 路徑、單實例/多實例策略
- MCP config 一行安裝範例（給任意 agent 用）
- 與 store/search/arbitration 的呼叫邊界
- 錯誤映射到 MCP 回應
- 優雅關閉、日誌（不含 secret）
- 介面草圖、驗收條件、開放問題、對齊聲明

先讀 DESIGN.md。
