# T-RG-D02 設計任務（僅設計、禁止實作）

> ⚠️ **本檔案為設計階段歷史 prompt（2026-07-21 設計凍結前）。** 下列規格中 `potion-base-8M` 與 `cosine ≥ 0.92` 已被 PPLX 審查裁決取代為 `potion-multilingual-128M` 與 `cosine ≥ 0.90`。**實作規格以 `DESIGN.md`（SOT）與 `docs/design/02-storage-search-dedup.md`（已更新）為準，不應參考本 prompt。**

你是艦隊成員，由 Command Tower 經 route() 指派（tier=opencode-deepseek-pro）。

## 硬性規則
1. **只寫設計文件，禁止修改 `src/`、`tests/` 的實作碼**（可讀）。
2. 語言：台灣式繁體中文。
3. 以 `DESIGN.md` 為 SOT；不耦合 herdr。
4. 完成後在檔案末寫 `## DONE`。

## 產出
寫入：`docs/design/02-storage-search-dedup.md`

## 內容必須涵蓋
- SQLite schema、FTS5 虛擬表、triggers、indexes（與 DESIGN 一致或標出建議修正）
- state 路徑 `~/.local/state/remagraph/`、權限、migration 策略
- store.py 公開 API（insert/update status/supersede/invalidate）
- search.py：BM25、tag/kind/status 過濾、排序、top_k
- dedup.py：model2vec potion-base-8M、cosine ≥ 0.92、回傳最相似 ID
- embedding BLOB 只存不查（v1）；v2 sqlite-vec 升級路徑
- 併發與 locking、失敗模式
- 介面簽名草圖、驗收條件、開放問題、對齊聲明

先讀 DESIGN.md。
