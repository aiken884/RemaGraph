{
  "mcpServers": {
    "remagraph": {
      "command": "remagraph",
      "args": ["serve"],
      "env": {
        "REMAGRAPH_STATE_DIR": "/Users/aikenlin/.local/state/remagraph"
      }
    }
  }
}
