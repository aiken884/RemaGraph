# RemaGraph 環境變數設定
# 複製此檔為 .env 並依需求修改。所有變數皆為可選，RemaGraph 有合理的預設值。

# State 目錄（SQLite 資料庫、audit.jsonl 存放位置）
# 預設：~/.local/state/remagraph/（Linux/macOS）或 $XDG_STATE_HOME/remagraph/
# REMAGRAPH_STATE_DIR=~/projects/my-project/.remagraph

# 資料庫檔案路徑（若不使用 state 目錄下的預設 remagraph.db）
# 預設：{state_dir}/remagraph.db
# REMAGRAPH_DB_PATH=/tmp/remagraph-test.db

# Unix socket 路徑（daemon 模式）
# 預設：{state_dir}/remagraph.sock
# REMAGRAPH_SOCKET_PATH=/tmp/remagraph.sock

# 日誌等級（DEBUG, INFO, WARNING, ERROR）
# 預設：INFO
# REMAGRAPH_LOG_LEVEL=DEBUG

# 日誌檔案路徑（stdio 模式預設輸出到 stderr；daemon 模式強制寫入檔案）
# 預設 daemon：{state_dir}/remagraph.log
# REMAGRAPH_LOG_FILE=/var/log/remagraph.log
